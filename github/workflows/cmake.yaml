name: Cmake

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: build and run test
        run: mkdir build
          cd build
          cmake ..
          make
          ctest
